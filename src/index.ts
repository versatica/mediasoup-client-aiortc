import { Worker } from './Worker';
import * as types from './types';

/**
 * Expose all types.
 */
export { types };

/**
 * Expose version.
 */
export const version = '__VERSION__';

/**
 * Expose Worker class.
 */
export { Worker };
